

#Paquetes disponibles

https://anaconda.org/anaconda/repo?type=conda

# Instsla<cion

Problema 
error while loading shared libraries: libz.so.1: failed to map segment from shared object: Operation not permitted


TMPDIR=$HOME/temp bash Anaconda3-2021.05-Linux-x86_64.sh -u


## 2 anacondas instalados

Inicialr el que queramos con 

/home/tjorajuria/anaconda3/condabin/conda init



/data/scratch/AIN/anaconda3/condabin/conda init

comrpobamos con which 
Acordarse del source en los scripts




# Anaconda Comandos1

## Descativar entorno

```
conda config --show | grep auto_activate_base
``` 
 

To set it false
'''
conda config --set auto_activate_base False
source ~/.bashrc
'''
To reactivate set it to True
'''
conda config --set auto_activate_base True
source ~/.bashrc
'''




Busqueda por plataforma

https://anaconda.org/search

Probar

source activate my_env && python my_script.py



There is also another way. You can directly use the python executable from environment in ExecStart like this:

ExecStart=/path/to/conda/envs/my_env_name/bin/python /path/to/executable

For my CentOS server where I'm using miniconda the path is:

ExecStart=/root/miniconda3/envs/test_env/bin/python3 /root/test.py

--------------------



#si no lo ha mtido en el path añadir $HOME/anaconda3/bin a .bash_profile
PATH=$PATH:$HOME/.local/bin:$HOME/bin:$HOME/anaconda3/bin

conda init
reiniciar sesion


conda config --set auto_activate_base false
conda info --envs

$ conda update -n base -c defaults conda


conda create --name naitecv2 --no-default-package
conda create --name gpuv1 --no-default-package
#otra opcion

conda create --name fastai python=3.6 --no-default-packages
conda create --name fastai --no-default-packages


conda activate pruebasv1
conda config --add channels https://public.dhe.ibm.com/ibmdl/export/pub/software/server/ibm-ai/conda/linux-ppc64le/

conda install python=3.6
numpy
numpy 1.8*
numpy 1.8.1
numpy >=1.8
numpy ==1.8.1
numpy 1.8|1.8*
numpy >=1.8,<2
numpy >=1.8,<2|1.9
numpy 1.8.1 py27_0
numpy=1.8.1=py27_0

conda install powerai 

conda remove -n nombre --all



#integrarlos los envs en jupyter

Conda install nb_conda


#Update conda

 conda update -n base -c defaults conda


problemillas con miniconda

conda update -n base -c defaults conda

conda init bash y volver a abrir consola


limpiar caches, paquetes descargados...
conda clean -a 

##Migrar entorno

conda list --explicit > spec-file.txt

conda create --name myenv --file spec-file.txt

o

conda install --name myenv --file spec-file.txt


##Export
#Activar primero el entorno 
conda env export > environment.yml
conda env create -f environment.yml

##Uninstall

### OPcion 1

Quitar la carpeta con rm -rf (generalmenet donde se ha ejecutad el script /anaconda3

MIrar parth como se ha quedadopor si hay que auitar la ruta

### Opcion 2

conda install anaconda-clean



Remove all Anaconda-related files and directories with a confirmation prompt before deleting each one:

anaconda-clean

Or, remove all Anaconda-related files and directories without being prompted to delete each one:

anaconda-clean --yes





#EJEMPLO 
# Para asegurarte tener el mismo entorno con las mismas versiones de paquetes, importa el entorno
condaenvcreate-f py36cdd.yml
# crear entorno desde 0 (podrían no coincidir las versiones de los paquetes)
condacreate--namepy36cdd python=3.6
activatepy36cdd
condainstalljupyternb_conda
condainstallnumpymatplotlibpandas scipyscikit-learn
condainstallimbalanced-learn--channelconda-forge
condainstallpyspark=2.3.0
pipinstalltensorflowkeras
condaupdate-n base -c defaults conda
# exportar entorno
condaenvexport--no-builds> py36cdd.yml